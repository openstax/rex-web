---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: alpine

inputs:
- name: rex-web-pull-request

run:
  path: /bin/sh
  args:
    - -c
    - |
      apk add bash git
      ./rex-web-pull-request/concourse/check-pr-size/script.bash
