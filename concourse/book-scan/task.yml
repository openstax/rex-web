---
platform: linux

image_resource:
  type: docker-image
  source:
    password: ((dockerhub-password))
    username: ((dockerhub-username))
    repository: node
    tag: v14.15.4

inputs:
- name: release-issue
- name: rex-web

caches:
- path: node_modules

run:
  path: rex-web/concourse/book-scan/script.bash
