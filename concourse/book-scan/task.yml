---
platform: linux

image_resource:
  type: docker-image
  source:
    password: ((dockerhub-password))
    username: ((dockerhub-username))
    repository: openstax/rex-web
    tag: CI-2021-09-21

inputs:
- name: cloudfront-environment

run:
  path: /code/concourse/book-scan/script.bash
